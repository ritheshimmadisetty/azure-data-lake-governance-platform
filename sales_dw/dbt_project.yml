name: 'sales_dw'
version: '1.0.0'

profile: 'sales_dw'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"


models:
  sales_dw:
    staging:
      +materialized: view
    dimensions:
      +materialized: table
    facts:
      +materialized: table
